# Changelog

## 2.6.1 — 2026-07-24

- 适配 Codex Desktop `26.721` 首页新增的 `home-banners` 结构；按 DOM 自动识别新旧布局，避免 QQ / 自定义皮肤下「新建任务」面板被顶出可视区域而空白。
- 修复切换到原生皮肤或 reinject 时 QQ 企鹅头像/侧栏 profile 残留的问题。
- 首页内容列改为稳定标记（`qq-skin-home-stack` / `dream-skin-home-stack`），旧版 Codex 无 banners 时仍走原注入路径。

## 2.6.0 — 2026-07-23

- 将右下角 Codex 伙伴从单一头像升级为可交互桌面房间，增加随时间、运行中、等待确认、完成、离线和休息状态变化的场景反馈。
- 电脑、书架、台灯和盆栽统一接入“GitHub 热门项目盲盒”；机器人先播放抽书动作，再随机展示热门仓库名称与中文项目简介。
- 项目盲盒新增收藏、打开 GitHub、换一本和“以后少推荐这种”；本地记录收藏、语言偏好与发现历史，候选充足时主动减少同类推荐。
- 连续发现 5 个项目会解锁房间摆件，内置地球仪、火箭、奖杯和小猫四种本地成长奖励。
- 新增 GitHub 公共仓库数据 worker、六小时本地缓存、中文简介翻译和中文离线兜底；移除早期实验中的 YouTube 与 Hacker News 数据链路。
- 修复热应用只注入房间 UI、未同步资讯快照的问题；一次性注入现在会直接携带本地缓存，不依赖后台 watcher 才能显示内容。
- 修复盲盒按钮在 Electron 中收不到真实鼠标点击的问题；四个操作分别使用 `pointerup + click` 双通道并去重。
- 增加项目数据、缓存迁移、一次性注入、盲盒结构、按钮绑定和五次发现解锁的 macOS/Windows 共用回归测试。

## 2.5.3 — 2026-07-23

- 修复 QQ 皮肤任务阅读列被嵌套查询容器压缩的问题；右侧摘要、审阅或工具栏打开前后保持 Codex 原生 48rem 宽度，窄窗口仍保留左右各 12px。
- 修复验证布局时自定义皮肤入口被错误置灰的问题；安装器内置的大黄蜂深度皮肤仍可从标题栏直接切换。
- macOS 与 Windows 应用内更新新增实时下载进度、已下载/总大小及百分比；下载较慢或失败时可直接打开对应 GitHub Release 手动下载。

## 2.5.2 — 2026-07-23

- 修复 Windows 超宽窗口的三栏任务布局：对话列现在为居中算法对称预留右侧摘要栏宽度，不再伸入成长中心或伙伴面板下方。
- Windows 原生安装器新增“一键应用内置大黄蜂皮肤”；V2 大黄蜂预设随 EXE 离线打包、安装时自动加入皮肤库，并增加 Windows 回归校验。
- 官网升级为 Codex 皮肤生成器产品页，展示真实大黄蜂皮肤案例并保留经典 QQ 介绍页。

## 2.5.1 — 2026-07-22

- 修复 QQ 标题栏切换器偶发吞掉“原生 / QQ / 自定义”点击的问题，增加可靠的指针事件激活路径。
- 新增独立的当前皮肤模式状态，使 Codex 与 macOS 安装器皮肤库的选中状态保持同步。
- QQ 或原生模式下不再错误勾选上一次使用的自定义皮肤；自定义菜单也只在自定义模式下显示当前皮肤勾选。
- 修复 `deep-custom` 深度皮肤被切换器误判为不可用的问题。
- 优化任务详情阅读区宽度：减少普通任务两侧空白，避免三栏内容伸入摘要面板，并修正分屏侧任务的双重扣宽与滚动条偏移。

## 2.5.0 — 2026-07-22

- 新增 `codex-deep-skin-builder`（Codex 深度皮肤助手）：用户只输入主题关键词即可自动生成内部参考方案、分层素材、V2 主题包并完成本地应用与真实验收；参考图改为可选输入。
- macOS 与 Windows 安装器新增单按钮“安装 Codex 深度皮肤助手”，安装、更新均为本地原子复制，不增加多步骤定制向导。
- 新增 `schemaVersion: 2` / `deep-custom` 通用渲染协议，支持背景、右侧前景、侧栏人物、水印、品牌徽标、头像、品牌文字、配色和受控布局变量。
- 新增 `.codexskin` 安全主题容器及 `create / validate / export / import / install / apply / verify` 命令；拒绝路径穿越、符号链接、未声明内容、损坏图片和超限资源。
- 大黄蜂深度主题从专用硬编码迁移为自包含 V2 预设；主题库和热切换同时兼容 V1 单背景主题与 V2 深度主题。
- 增加确定性打包、往返导入、非法内容拒绝、通用 renderer 和 Skill 结构回归测试。
- 修复安装器将“版本不同”误判为“需要更新”的问题：远端或内置版本必须严格高于当前版本才允许更新；同版本不重复提示，本地版本更高时明确禁止降级。
- 自动更新进一步校验精确资产名、SHA-256、Release/安装包版本一致性和 macOS 代码签名；替换失败或复制后签名失败会回滚原 App。
- macOS 与 Windows 安装器为深度皮肤助手增加三态检测：未安装时显示安装入口、内置 Skill 较新时显示更新入口、内容一致时显示“已安装”并给出一句话用法，不再无条件显示更新。

## 2.4.0 — 2026-07-22

- QQ 模式右侧上方新增“Codex 成长中心”，展示本机今日、近 7 天、历史累计 token、七日趋势、活跃天数与连续活跃天数。
- 新增 QQ 风格等级：活跃日提供基础成长，包含缓存的总 token 提供每日封顶加速，以星星、月亮、太阳、皇冠四进制展示；界面默认展示总用量，并提供“净用量”开关排除缓存。
- 成长中心默认视觉替换原生摘要面板，但不删除原生 DOM；“资料 / 成长统计”按钮可以真实双向切换。
- 新增 Codex 专用本地日志解析器，支持 fork、子 Agent、归档与重复 token 去重以及增量缓存，不包含账号、API Key 或网络同步模块。
- 增加 `usage-snapshot-v1` 校验、后台 worker 超时隔离、60 秒增量刷新、手动刷新、加载/空/错误态和 macOS/Windows 共用回归测试。

## 2.3.1 — 2026-07-22

- 修复 QQ 模式下右下角“聊天”浮窗背景透明、正文与底层任务内容重叠的问题。
- 聊天浮窗打开时自动隐藏右侧摘要栏与 Codex 伙伴，关闭后自动恢复，避免浮层互相遮挡。
- QQ 顶部菜单新增“技能”按钮；在非插件页面点击时会先进入插件页，再触发原生技能页切换。
- 修复 QQ 菜单“插件”误点侧栏入口的问题；插件与技能现在都操作原生顶部 Tab，可连续双向切换。

## 2.3.0 — 2026-07-22

- macOS 与 Windows 原生应用启动后自动检查 GitHub 最新 Release；仅在发现更高版本时提示，由用户决定是否升级。
- 用户确认后在应用内下载安装包，并强制核对同一 Release 的 SHA-256 校验文件；校验失败不会运行或替换应用。
- macOS 自动解压、安全替换并重新打开 App，失败时保留旧 App 回滚；Windows 校验后启动新版安装程序。
- macOS 构建现在同步生成 `Codex.QQ.Skin.app.zip` 与 `.sha256`，与自动更新器的 Release 资产约定保持一致。

## 2.2.2 — 2026-07-22

- QQ 模式强制使用 Codex `electron-light` 完整浅色外观，并在离开 QQ / 卸载皮肤时原样恢复用户先前的原生配色。
- QQ 标题栏蓝色填充延伸到右缘，避免被模式切换器截断；浅色摘要面板覆盖系统深色残留。
- 自定义壁纸改为轻柔化（1px）加淡遮罩，保留人脸与物体细节；修复 Codex 2026.7 首页垂直居中产生的顶部空隙。

## 2.2.1 — 2026-07-21

- macOS App 改为「启动 / 皮肤库」双 Tab；皮肤库每行提供应用、重命名、删除，去掉底部批量按钮与多余的刷新 / 打开文件夹 / 重新安装入口。
- 启动成功后不再弹出「完成」确认框，状态栏提示即可。
- 自定义皮肤窗口背景统一模糊并加淡遮罩，避免原图过锐影响文字可读性。
- 新增 `rename-theme-macos.sh`；窗口高度与列表默认五行可视高度收紧。

## 2.2.0 — 2026-07-21

- **皮肤管理器第二阶段：** macOS App 作为皮肤库主入口，支持列表、应用选中、删除、刷新与打开库文件夹。
- Codex 右上角在「自定义」旁增加轻量 ▾ 菜单，仅列出最近自定义皮肤；完整增删改仍在 App。
- 新增 `list-themes-macos.sh` / `remove-theme-macos.sh`；注入器注入库列表并通过 `localStorage` 通道热切换（`switch-theme --no-apply`，不重启注入进程）。
- QQ 皮肤仍保持封闭产品形态，不与用户图片混用。

## 2.1.3 — 2026-07-21

- macOS App 启动前自动比对内置版本与 `~/.codex/codex-qq-skin-studio`；若引擎仍是旧版或仍含 `MODE_ARGS[@]`，会先刷新再启动，避免只换 App 却继续报「操作没有完成」。
- 对 `MODE_ARGS` / `unbound variable` 给出更明确的中文提示，引导点击「重新安装 / 更新」。
- **QQ / 自定义硬隔离：** 切到 QQ 时清除 `--dream-skin-art`、不再注入 `custom-skin.css`、忽略上传图的 art 分析；切到自定义时清除 `--qq-skin-art`。两套皮肤不再混用背景。
- 修复 QQ 复古标题栏拖动区域挡住皮肤切换按钮，导致无法切回「原生 / 自定义」。
- 皮肤切换条右移避让，避免盖住 Codex 原生文件夹入口。

## 2.1.1 — 2026-07-21

- 修复 macOS `/bin/bash 3.2` 在 `set -u` 下展开空数组 `${MODE_ARGS[@]}` 导致 GUI「操作没有完成」误报（皮肤其实已启动）。
- 启动 Codex 时附带 `--lang`（读取 `localeOverride` 或系统语言），减轻安装后界面变成英文的问题。
- 复古标题栏/工具栏恢复 Electron `-webkit-app-region: drag`，修复皮肤覆盖原生拖动条后窗口无法拖动。
- 将调试端口相关失败提示改为中文，降低「CDP 端口设置有问题」类困惑。

## 2.1.0 — 2026-07-21

- 新增原生 C# WinForms Windows 安装器，提供“一键安装并启动”和“上传图片生成皮肤”两个入口，并内置独立 Node.js 运行时。
- 支持 Microsoft Store/MSIX 版 ChatGPT，通过 Windows 应用激活接口携带本地 CDP 参数启动。
- 修复 Windows 升级时旧注入器占用安装目录、单进程严格模式计数、启动验证竞态和安装器完成状态不刷新的问题。
- Windows 标题栏、任务栏和 EXE 统一使用与 macOS 相同的项目图标。
- 优化 Windows QQ 界面：设置导航、标题靠左、工具栏按钮位置、顶部空白和伙伴面板显示状态。
- 自定义图片改为窗口级 `cover` 背景，新建任务、任务详情和左侧栏连续铺满同一张图片。
- Windows 安装器和注入 payload 已在真实 ChatGPT MSIX 客户端上完成注入验证。

## 2.0.0 — 2026-07-20

- 原生 macOS App 新增“上传图片，生成我的皮肤”入口；用户选择图片并命名后即可自动生成并立即应用整套 QQ 皮肤。
- 图片只在本机处理：自动分析主色、亮度、视觉焦点、左右安全留白、画面比例与任务页呈现方式，不上传到任何服务器。
- 自定义主题不再写死默认强调色，确保图片分析得到的协调配色可以直接驱动标题、描边、卡片和背景材质。
- Windows 10/11 x64 包新增图片选择入口，与 macOS 共用同一套自适应皮肤引擎。
- 修复从“一键切换”停留在原生界面后上传图片会保存成功但注入验证失败的问题；明确的上传、应用或启动操作现在会重新启用皮肤，后台热更新仍尊重用户之后的切换选择。
- 重构为三套互相独立的皮肤模式：Codex 原生、固定 QQ 皮肤、用户自定义皮肤；右上角使用三段式切换器，上传成功后默认选择“自定义”。
- 用户图片不再进入 QQ 复古外框、三栏布局和伙伴卡，而是沿用 Codex 原生结构，参考 Dream Skin 的本地图像分析与首页构图规则生成沉浸式新建任务页。
- QQ 皮肤固定使用内置主题配置与 `portal-hero.png`，不会再被用户上传图片或自定义配色覆盖。
- 人物及普通照片在新建任务顶部构建框中改用完整适配，避免横幅比例裁掉头部或身体；超宽背景图仍使用铺满模式。
- 修复从自定义皮肤切回 QQ 时只恢复外框的问题：现在会重新读取 QQ 的固定蓝色配色和三栏布局，并恢复左栏、右侧摘要托盘与 Codex 伙伴面板。
- 兼容新版 Codex 将固定摘要开关重命名为“显示/隐藏侧边栏”的 DOM，使用真实的右上角坐标区分左侧导航开关。

## 1.8.1 — 2026-07-20

- 明确皮肤切换按钮的双向文案：QQ UI 显示“切换原生皮肤”，原生 UI 显示“切换 QQ 皮肤”，并由回归测试锁定。
- 项目介绍网页更新为同时支持 macOS 与 Windows，不再只描述 macOS。
- 网页加载时尝试播放内置咳嗽提示音；若浏览器阻止有声自动播放，则在首次点击、触摸或按键时补播。
- 重新构建 macOS APP 与 Windows x64 便携发布包。

## 1.8.0 — 2026-07-20

- 新增 Windows 10/11 x64 预览版运行层，包含安装、启动、验证、暂停和安全恢复 PowerShell 脚本。
- Windows 发布 ZIP 内置经过官方 SHA-256 校验的 Node.js 便携运行时，普通用户不需要另行安装 Node.js。
- 新增 GitHub Actions Windows Server Runner：自动检查 PowerShell 语法、共享注入测试、Windows 配置备份隔离、发布包内容与 SHA-256。
- Windows 版沿用 loopback CDP、Codex 进程后代核验和记录进程身份核验，不修改官方安装目录或 `app.asar`。
- 新增 Windows 使用说明和真实 Windows 10/11 验收边界；macOS 稳定版运行流程保持不变。

## 1.7.0 — 2026-07-20

- 标题栏右侧新增常驻“一键切换”按钮，可在 QQ 皮肤与 Codex 原生 UI 之间即时往返；原生模式仍保留入口，并记住用户上次选择。
- 切换按钮在 QQ 模式采用蓝银玻璃渐变，在原生模式采用浅色系统胶囊并向左避让文件夹入口。
- 新增原生 macOS `Codex QQ Skin.app`，提供无需终端的一键安装、启动、更新和恢复入口。
- 新增 Intel 与 Apple Silicon 通用 APP 构建脚本，以及可选 Developer ID 签名流程。
- README 增加 APP 安装方式和未公证版本的 Gatekeeper 图形界面放行步骤。
- 兼容新版 Codex 设置页 DOM：将完整双栏设置内容移至复古工具栏下方，并仅隐藏不含交互控件的原生占位层，避免“返回应用”和详情标题被顶部装饰遮挡。
- Codex 伙伴新增“打开宠物 / 终端 / 提示音”三个快捷按钮、任务状态提示和本周剩余用量百分比；额度读取 Codex 原生周用量并缓存最近一次有效值。
- 周用量优先读取 Codex 当前账号的实时七天限制数据，并按账号 ID 隔离缓存；切换账号后立即刷新，不再沿用上一账号的额度，尚未读取到新账号真实用量时显示“--”。

## 1.6.1

- 用耳聆网页面标注为 CC0 的“QQ系统消息提示音”替换任务完成时的合成咳嗽声，并保留来源、许可声明和 SHA-256 记录。
- 合成咳嗽仍作为浏览器音频文件播放失败时的后备提示。

## 1.6.0 — 2026-07-19

### 提示音

- 新增本地实时合成的两套原创提示音：任务完成播放双段“咳咳”声，请求授权播放四段急促“滴滴”声，不捆绑或下载 QQ 官方音频。
- 基于 Codex 运行按钮状态转换和授权操作卡片识别触发，支持同一授权去重、手动停止抑制、路由切换与热更新防误报。
- Codex 伙伴卡新增持久化声音开关；主题可配置开关、音量以及完成/授权音色。

### 启动稳定性

- 修复根观察器监听皮肤自身内联样式所形成的高频刷新反馈环，避免启动时 React 原生布局与皮肤重复争抢尺寸。
- 启动后的前四秒增加有界布局稳定检查和原生 resize 通知，输入框与左下在线用户卡无需再切换终端面板才能完整出现。
- 移除会把原生导航拉到复古工具栏下方的 `-46px` 负位移；侧栏和主内容现在从完整 70px 标题区之后开始，新建任务、首页头图与顶部导航不再被裁切。

## 1.5.4 — 2026-07-18

### 交互与布局

- 任务内容区增加输入框安全间距，最后一条任务信息不再被粘性输入模块的圆角和阴影遮挡。
- 复古顶部工具栏改为真实按钮，并代理到 Codex 原生的新建任务、已安排、插件、站点、拉取请求和 Quick chat 入口。
- 右侧蓝色共同外框从窗口顶部贯穿到底部，同时包住输出/来源与 Codex 伙伴两个模块。

## 1.5.3 — 2026-07-18

### 细节优化

- 首页主问题强制保持单行，并扩大可用文字宽度；四个快捷入口的图标严格居中。
- 首页说明改为 QQ 主题幽默口号：“滴滴！QQ 皮肤已上线，今天也和 Codex 一起把 Bug 聊下线。”
- 项目/任务标题只让精确的栏目行获得渐变背景，省略号与加号悬停时不再误刷额外背景条。
- 设置页移除“返回应用”上方 46px 空白，同时保留按钮原生点击布局，避免透明标题层拦截返回操作。
- 右侧蓝色托盘延伸至标题区顶部；输出/来源视觉居中并与 Codex 伙伴留出稳定纵向间隔，伙伴卡四周保留对称留白。
- 热应用继续先清理所有已知旧 watcher，再只启动一个安装目录 watcher，避免旧内存 CSS 覆盖新版本。
- 一次性验证、移除和截图命令完成后主动退出，不再因空闲 CDP 句柄残留为第二个 injector 进程。

## 1.5.1 — 2026-07-18

### 修复

- 复古外框新增贯穿标题栏到底部的独立左右蓝色边轨，右栏输出/来源与伙伴卡不再和底图边框重叠。
- 仅在真实任务路由移除 Codex 原生 46px 内容偏移，左栏、中间对话和右栏整体紧贴工具栏；设置页不再被任务页尺寸规则挤掉详情区。
- 左栏“置顶 / 项目 / 任务”分组条强制铺满可用宽度，消除右侧截断。
- 设置页出现时立即隐藏任务伙伴卡，保留完整设置导航与详情内容。
- 右上三颗控件改用 Codex 原生按钮的 SVG、尺寸与交互，不再显示仿 Windows 的最小化、最大化和关闭按钮。

## 1.5.0 — 2026-07-17

### 视觉

- 将被纵向放大的单层标题图改为 38px 深蓝标题行与 29px 蓝银工具行，两行共同组成 72px 复古标题区，并与左右/底部窗框严密贴合。
- 右上不再绘制放大的假按钮：直接识别并装扮 Codex 当前可见的三颗原生按钮，固定为 28×28，并保留真实点击行为。
- 标题企鹅与文字整体右移，避开 macOS 交通灯；标题企鹅改用独立透明素材，缩放时不再随整张标题栏变形。
- 左下原生用户入口增加 QQ 风格企鹅头像、用户名和绿色“在线”标记；覆盖层不接管点击，原生账户菜单继续可用。
- Codex 伙伴卡动态读取右上原生输出/来源面板宽度，两块右栏现在完全同宽、同右边线。

### 兼容

- 标题、工具栏和原生按钮位置均由 CSS/实时 DOM 生成，透明外框仅绘制四周边线；窗口尺寸变化不会再放大企鹅和按钮。

## 1.4.0 — 2026-07-17

### 视觉

- 新增完整的「Codex 2007」拟真窗体：内容区顶部显示固定高度的深蓝旧式标题栏、原创像素企鹅、最小化/最大化/关闭装饰按钮与动态任务标题。
- 根据参考图生成独立透明外框素材，并通过九宫格边框绘制四周蓝银倒角；标题栏纹理独立裁切，窗口缩放时只拉伸中段，不会压扁企鹅、按钮或四角。
- Codex 原生内容整体内缩到复古窗框内部，现有左栏、中间对话、右上原生输出/来源和右下伙伴卡保持真实可交互内容。

### 安全与兼容

- 复古标题栏是 renderer 内的无交互视觉层，不修改官方 App、`app.asar`、签名或 macOS 原生交通灯标题栏；窄窗口会自动缩短标题栏并隐藏过长标题。
- 新增外框与标题栏素材的 payload、热更新、doctor、清理和对象 URL 生命周期检查。

## 1.3.1 — 2026-07-17

### 视觉

- 经典三栏的左侧栏改为蓝银分组工作台：置顶/项目标题使用旧式渐变栏，每个原生项目形成文件夹卡片，每个任务拥有独立浅蓝背景、描边与当前项高亮。
- 中间对话区按真实 `data-turn-key` 将每轮用户与 Codex 内容放入白蓝对话卡；用户消息保留独立气泡，回复、代码块、行内代码和输入框使用接近参考图的矩形蓝色描边。
- 任务页主背景从大面积淡蓝改为更接近旧式聊天窗口的珍珠白，降低长回复阅读时的底色干扰。

### 兼容

- 新样式只在浅色经典三栏任务页启用，并只依赖 Codex 的稳定 action / conversation 数据属性；首页、窄窗口和其他外观继续沿用原规则。

## 1.3.0 — 2026-07-17

### 新增

- 新增「经典 Codex 三栏」：宽屏任务页自动打开 Codex 原生固定摘要，右侧上方继续显示真实的输出、来源、进度和子代理内容，下方显示独立 Codex 伙伴卡；不复制原生数据，也不修改官方 App。
- 新增原创透明 Codex 伙伴素材与蓝银经典预设；全新安装默认启用该预设，已有活动主题保持不变。
- 自定义主题新增 `layout.mode`、`layout.rightPanel`、`layout.minWidth` 与 `layout.rightWidth`，可关闭三栏、记忆原生面板状态或调整启用阈值和右栏宽度。

### 改进

- 原生固定摘要在三栏模式下缩短为右上模块，为下方伙伴卡预留独立空间；小于阈值、首页或找不到原生开关时自动退回原布局。
- watcher 将伙伴图片纳入静态 payload 哈希与热更新，暂停、恢复和重新注入时同步清理对象 URL 与装饰节点。

### 测试

- 新增原生摘要只自动打开一次、伙伴仅随真实固定摘要出现、布局配置校验、素材打包和清理回归测试；doctor 改为隔离 HOME 运行，避免依赖用户当前主题状态。

## 1.2.0 — 2026-07-17

### 新增

- 自适应图像主题引擎：通过本地 Canvas 分析亮度、主色、视觉焦点、左右安全区和图像比例，为任意背景图生成协调的浅色/深色外观；图片不会上传到外部服务。
- 主题新增 `appearance: auto | light | dark` 与 `art.focusX/focusY`（`0..1`）、`art.safeArea: auto | left | right | center | none`、`art.taskMode: auto | ambient | banner | off` 配置；显式值优先于自动分析。
- 首页与任务页按图像比例采用不同呈现：超宽图在任务页使用横幅和纵向渐隐，普通比例图使用低噪环境背景，也可用 `taskMode=off` 关闭任务页图像。
- 内置「桥本有菜」实测精选预设与 5 套可复现的程序化抽象预设；安装后幂等播种到主题库，菜单栏或 `switch-theme-macos.sh` 可直接切换，且绝不覆盖 `custom-*` 用户主题。
- 新增中英文参考图生图指南：明确 `2560 × 1440`、Image 1/2/3 职责、裁切安全区、原创/已授权成年身份两套流程，以及“文案和小照片不烘焙进背景”的约束。

### 改进

- watcher 在文档壳层出现后立即注入，按 CSS/主题/图片内容哈希热刷新并复用静态 payload；同一主题切换不再重复启动守护进程，减少原生界面闪现和后台轮询。
- 主题切换先完整暂存图片，最后原子发布 `theme.json`；全新安装在没有活动主题时先启用通用的「午夜极光」，已有活动主题保持不变。
- `load-image-theme-macos.sh` 可通过 `--appearance`、`--focus-x`、`--focus-y`、`--safe-area`、`--task-mode` 精确调节构图；旧主题缺省时使用安全自适应值。

### 修复

- 保留 Codex 原生固定顶栏的定位与层级，避免打开任务侧边面板后开关被推出主区、导致面板无法关闭。
- 修复亮色背景图在 ChatGPT/Codex 暗色模式下错误生成浅色皮肤壳的问题。`appearance=auto` 现在跟随原生/系统外观，避免白字叠在浅色面板上导致界面不可读。
- 修复从“设置 > 外观”返回“已安排的任务”等无输入框路由后，验证器因找不到 composer 而拒绝合法 Codex 主界面的问题。
- 首页不再在原生标题栏注入主题名称和圆形伪按钮；watcher 为后续完整导航注册更早的注入并缩短目标探测间隔，以减少原生界面先闪现再换肤的时间。
- 超宽任务横幅不再让背景伪元素在固定高度直接结束；图片仍按原比例置顶，遮罩与渐隐延续到整页，减少明显的底部截断感。
- 修复普通宽屏图片被 `contain` 与 `cover` 双层重复绘制的问题；标准 16:9 现在始终使用一张 `cover` 整窗背景，并按安全区和焦点保留主体。
- 修复沉浸任务页侧栏原生缩放热区继承背景后向主区延伸 20px、形成明显竖向分割的问题；同时统一顶栏、侧栏与输入框的半透明材质，并为输入框补上不受原生边框宽度影响的内描边。
- 16:9 宽图现在在首页也使用单张整窗背景，不再把同一图片重复绘制成 hero 卡片；插件、已安排和 Pull Requests 等工具路由会清除原生整块黑底，并将搜索框和撰写器统一为单层表面。任务页同时移除独立顶栏底色和撰写器后的原生底部渐变，避免顶部、底部出现重叠面板。
- SwiftBar 菜单栏标题恢复调色板图标，并加入静态回归检查，避免升级后只剩 `Skin ON` 文本。
- 浅色模式撰写器改为更通透的珍珠白表面，并修复占位文字被原生双重透明度削弱的问题；暗色模式继续使用单层实色表面。
- 兼容 Codex Desktop 更名：官方桌面端在 26.707 从 `Codex.app` 更名为 `ChatGPT.app`（bundle id 仍是 com.openai.codex）。发现 / 启动流程现在两种名字都识别，且 `state.json` 缓存的旧 app 路径若已不存在则不再劫持启动——此前更新后会因指向旧 `Codex.app` 而启动失败
- 菜单栏与 `status-qq-skin-macos.sh` 不再依赖 `/usr/bin/python3`（macOS 12.3+ 默认不预装）读取主题名与运行状态，改用纯 shell 解析；此前在未装 Xcode 命令行工具的机器上，主题名会退化成 id、`--json` 状态直接失效

### 安全

- CDP 端点必须由已验证的官方 Codex 可执行文件或其子进程监听；WebSocket 还会校验 loopback、page ID、路径、无重定向，并安全处理畸形消息和发送异常。
- 主题配置与图片使用真实路径 containment，拒绝 symlink 越界、空文件、超过 16 MB、单边超过 16384 px 或超过 50 MP 的图片；主题展示文本拒绝换行和控制字符。
- AppleScript 动态内容全部通过 argv 传递；SwiftBar 过滤主题 ID、文件名和菜单文本，避免主题元数据改变菜单属性或命令参数。
- `config.toml` 只按严格 UTF-8 读取，拒绝 NUL、歧义多行 TOML、重复 `[desktop]`，通过用户级锁、原始字节核验和同目录原子替换保护中文配置与并发写入。
- 暂停和恢复会在 TERM/KILL 前后核验 watcher 的 PID、启动时间、Node 和 injector 路径；不匹配的进程绝不结束，已确认 watcher 若无法停止则保留 state 并中止，不再报告假成功。

### 测试

- 覆盖每套预设的可注入性与播种幂等、首页/任务 renderer、早期注入、主题原子切换、中文与 CRLF/BOM 配置往返、非法 UTF-8/NUL/TOML 拒绝、路径穿越、symlink 越界、控制字符和像素炸弹。

### 说明

- 「桥本有菜」源图、标准化背景和浅/暗实机截图分别归档；截图只作预览，不能当背景导入。用户提供的图像不自动获得 MIT 素材许可，公开再分发前仍需确认相应权利。

---

## 1.1.2 — 2026-07-16

### 修复

- 修正内置主题引用了未随仓库发布的背景文件，恢复使用 bundled abstract demo 素材
- 更新主题配置往返测试：安装只备份外观键，不再错误断言强制切换深色模式
- 恢复原本没有 `[desktop]` 配置段的用户设置时，不再额外写入空段
- 热切换读取运行状态时复用 Codex 内置 Node.js，不再依赖系统 `python3` 或执行 `eval`
- 显式传入的 `--theme-dir` 缺少 `theme.json` 时立即报错，不再静默回退到内置 demo 主题

---

## 1.1.1 — 2026-07-16

### 修复

- 不再用 `launchctl submit` 托管带调试口的 Codex：退出 SwiftBar / 关掉 Codex 后不应再被 launchd 自动拉起
- 暂停与完全恢复时清理 `com.openai.codex-qq-skin-studio.app` 作业

---

## 1.1.0 — 2026-07-16

### 新增

- SwiftBar 菜单栏入口（`Install Menu Bar.command`）：应用 / 暂停 / 换图 / 切换已保存主题 / 从图片文件夹加载 / 完全恢复
- 主题库（`themes/`）与图片投放目录（`images/`）动态加载，不再把 README 图库合成图当背景素材
- 按 Codex 应用浅色 / 深色自动切换皮肤壳（`data-dream-shell`）

### 改进

- CDP 已就绪时热切换主题（重启 injector + 短时注入），换图更快
- 注入校验放宽（项目选择器等可选），避免误杀已生效皮肤
- 注入守护优先 `nohup`；暂停状态与路径大小写下停止逻辑更稳
- 安装时不再强制 `appearanceTheme=dark`，只备份桌面外观相关键，便于恢复与自动适配

### 视觉

- 以原版暗色 portal CSS 为结构底，叠加 light 壳与更薄横幅遮罩，减轻「换图看不清」
- 示例纯横幅：`docs/images/banner-arina-hashimoto-pure-no-ui.png`（无人机 UI 合成）

### 说明

- `docs/images/gallery/` 仅为效果预览，不要当 `theme` 背景导入

---

## 1.0.0 — 2026-07-15

- 发布 macOS 通用主题制作器，而不是固定角色皮肤。
- 加入 Finder 选图、自动 JPEG 转换、主题命名和高级配色参数。
- 主页使用独立横幅，任务页使用背景与磨砂层，完整保留原生交互。
- 改为复用并验证 Codex 官方签名 Node.js，不再附带大型运行时或依赖全局 Node。
- 增加独立安装目录、桌面启动/定制/验证/恢复入口。
- 增加官方签名、CDP 端口归属、PID 身份、刷新重注入和真实 DOM 自检。
- 增加原子配置备份、精确恢复、静态测试、安装恢复循环和发布打包脚本。
- 清理固定角色内部命名；传送门主题仅作为可替换示例素材。
- 开源树：示例横幅改为无角色抽象几何图；验收截图不入库；补充 NOTICE / README 商标与安全边界说明。
